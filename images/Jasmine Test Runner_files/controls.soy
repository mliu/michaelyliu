// This file was automatically generated from controls.soy.
// Please don't edit this file by hand.

goog.provide('ck.templates.controls');

goog.require('soy');
goog.require('soydata');


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.message = function(opt_data, opt_ignored) {
  return '<div class="message_container"><div class="message"></div><a class="timeout_link"></a></div>';
};


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.throb = function(opt_data, opt_ignored) {
  return '<span class="action_pending"></span>';
};


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.icon = function(opt_data, opt_ignored) {
  return '<span class="icon ' + soy.$$escapeHtml(opt_data.iconClass) + '"></span>';
};


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.panel = function(opt_data, opt_ignored) {
  return ck.templates.controls.message(null) + '<button class="rs-btn rs-btn-primary save">' + soy.$$escapeHtml(opt_data.labels.save) + '</button>' + ck.templates.controls.throb(null) + '<a href="#" class="rs-btn rs-btn-link cancel">' + soy.$$escapeHtml(opt_data.labels.cancel) + '</a>' + ((opt_data.iconClass) ? ck.templates.controls.icon(opt_data) : '');
};


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.closePanel = function(opt_data, opt_ignored) {
  return '<button class="rs-btn save" style="display:none"></button><button href="#" class="rs-btn cancel">Close</button>';
};


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.wizardPanel = function(opt_data, opt_ignored) {
  return ck.templates.controls.message(null) + '<button class="rs-btn rs-btn-secondary go-to-previous-step">' + soy.$$escapeHtml(opt_data.labels.previousStep) + '</button><button class="rs-btn rs-btn-primary go-to-next-step">' + soy.$$escapeHtml(opt_data.labels.nextStep) + '</button><button class="rs-btn rs-btn-primary save">' + soy.$$escapeHtml(opt_data.labels.save) + '</button>' + ck.templates.controls.throb(null) + '<a href="#" class="rs-btn rs-btn-link cancel">' + soy.$$escapeHtml(opt_data.labels.cancel) + '</a>';
};


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.boundPanel = function(opt_data, opt_ignored) {
  opt_data = opt_data || {};
  return ck.templates.controls.message(null) + '<button class="rs-btn rs-btn-primary save" data-bind="text: saveButtonLabel"></button>' + ck.templates.controls.throb(null) + '<a href="#" class="rs-btn rs-btn-link cancel" data-bind="text: cancelLinkLabel"></a>' + ((opt_data.iconClass) ? ck.templates.controls.icon(opt_data) : '');
};


/**
 * @param {Object.<string, *>=} opt_data
 * @param {(null|undefined)=} opt_ignored
 * @return {string}
 * @notypecheck
 */
ck.templates.controls.disabledPanel = function(opt_data, opt_ignored) {
  return ck.templates.controls.message(null) + '<button disabled="disabled" class="rs-btn rs-btn-primary save">' + soy.$$escapeHtml(opt_data.labels.save) + '</button>' + ck.templates.controls.throb(null) + '<a href="#" class="rs-btn rs-btn-link cancel">' + soy.$$escapeHtml(opt_data.labels.cancel) + '</a>';
};
